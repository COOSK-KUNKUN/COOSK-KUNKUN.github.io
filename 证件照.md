# 证件照制作 - 双入口上传改造

## 目标
将证件照制作的上传步骤从单一入口改为两个并排卡片入口：
- **自动去背景**：上传原照 → AI 去背景 → 编辑（现有流程不变）
- **已去背景照片**：上传已去好背景的照片 → 跳过 AI 处理 → 直接进入编辑

## 涉及文件
1. `js/tools/id-photo.js` — 主逻辑，改造上传步骤 UI 和文件处理流程
2. `css/style.css` — 新增双卡片布局样式

## 实现步骤

### 1. 修改上传步骤 HTML（`id-photo.js` mount 函数）

将 `#stepUpload` 内的单个 `.upload-area` 替换为两个并排卡片容器：

```
<div class="id-photo-upload-cards">
  <!-- 卡片 A：自动去背景 -->
  <div class="id-photo-upload-card" id="uploadAuto">
    <div style="font-size: 2.5rem;">🤖</div>
    <div class="id-photo-upload-card-title">自动去背景</div>
    <div class="id-photo-upload-card-desc">上传照片，AI 自动去除背景</div>
  </div>
  <!-- 卡片 B：已去背景 -->
  <div class="id-photo-upload-card" id="uploadManual">
    <div style="font-size: 2.5rem;">✅</div>
    <div class="id-photo-upload-card-title">已有透明底照片</div>
    <div class="id-photo-upload-card-desc">跳过 AI 处理，直接进入编辑</div>
  </div>
</div>
<input type="file" id="fileInputAuto" accept="image/*" style="display: none;">
<input type="file" id="fileInputManual" accept="image/png, image/webp" style="display: none;">
```
问一下
- 卡片 A 点击触发 `#fileInputAuto`（接受所有图片格式）
- 卡片 B 点击触发 `#fileInputManual`（限制为 PNG/WebP，因为只有这些格式支持透明通道）
- 两个卡片都支持拖拽（通过 dataTransfer 区分模式）

### 2. 修改 state 和文件处理逻辑

在 `state` 中新增字段：
```js
mode: null,  // 'auto' | 'manual'
```

修改 `initUI` 中的事件绑定：

```js
// 卡片 A：自动去背景
uploadAuto.addEventListener('click', () => fileInputAuto.click());
uploadAuto.addEventListener('dragover/drop', ...);  // 拖入时 state.mode = 'auto'
fileInputAuto.addEventListener('change', () => { state.mode = 'auto'; handleFile(file); });

// 卡片 B：已去背景
uploadManual.addEventListener('click', () => fileInputManual.click());
uploadManual.addEventListener('dragover/drop', ...);  // 拖入时 state.mode = 'manual'
fileInputManual.addEventListener('change', () => { state.mode = 'manual'; handleFile(file); });
```

### 3. 分支处理流程

修改 `handleFile` 后的处理分支：

- **`state.mode === 'auto'`**：走现有 `processImage()` 流程（AI 去背景 + 人脸检测 → 编辑）
- **`state.mode === 'manual'`**：新增 `processManual()` 函数：
  1. 用 `imageToData(state.img)` 将上传图像直接转为 ImageData（保留 alpha 通道）
  2. 赋值 `state.fgData = fgData`
  3. 可选：尝试人脸检测（在原始 RGB 上做，帮助自动定位）
  4. 直接进入 `showStep('edit')`，设置 editor 图像和参考线

### 4. 更新描述文案

将 `mount` 中的 `<p class="id-photo-desc">` 更新为：
> 上传照片制作标准证件照。可选择 AI 自动去背景，或直接上传已去好背景的照片。

### 5. 重置逻辑

`resetAll()` 中补充 `state.mode = null`，重置两个 file input 的 value。

### 6. CSS 样式

在 `css/style.css` 的 id-photo 区块新增：

```css
.id-photo-upload-cards {
    display: flex;
    gap: 1rem;
}

.id-photo-upload-card {
    flex: 1;
    border: 2px dashed var(--border-color);
    border-radius: var(--radius-md);
    padding: 2rem 1.5rem;
    text-align: center;
    cursor: pointer;
    transition: var(--transition);
    background: var(--bg-card);
    color: var(--text-secondary);
}

.id-photo-upload-card:hover,
.id-photo-upload-card.dragover {
    border-color: var(--accent-color);
    background: rgba(0, 113, 227, 0.05);
    color: var(--accent-color);
}

.id-photo-upload-card-title {
    font-weight: 600;
    margin: 0.75rem 0 0.25rem;
    font-size: 1rem;
}

.id-photo-upload-card-desc {
    font-size: 0.8125rem;
    opacity: 0.7;
}
```

## 验证
1. 点击「自动去背景」卡片 → 选择原照 → 走 AI 去背景流程 → 进入编辑（与现有行为一致）
2. 点击「已有透明底照片」卡片 → 选择 PNG → 跳过 AI → 直接进入编辑，透明区域显示棋盘格/底色
3. 拖拽文件到对应卡片也能正确触发对应模式
4. 「重新上传」按钮回到上传步骤，两个卡片正常显示
5. 卡片 B 只能选择 PNG/WebP 格式文件