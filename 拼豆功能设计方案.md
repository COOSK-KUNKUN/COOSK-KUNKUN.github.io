# 拼豆图纸生成器 — 落地设计方案

> 定位：在现有工具箱新增「拼豆图纸」工具。参考 `拼豆参考/` 的两个开源项目验证可行性，
> 但**不照搬**——按我们自己的工具箱架构和 github.io 静态部署约束重新设计，只做我们需要的。

## 一、可行性结论

可以落地，纯前端即可。参考项目已验证核心链路（上传→像素化→色号映射→导出）全部能在浏览器端完成：
- 像素化算法（Dominant 主色 / Average 平均色两种模式）是纯函数，可直接移植。
- 291 色的拼豆色板 + 5 套色号系统对照表已有现成数据（`colorSystemMapping.json`）。
- Oklab 色彩空间的颜色距离计算是小段纯函数，移植成本低。

不做的部分：AI 图片优化（参考的 `perler-beads-ai` 依赖火山引擎后端 API，静态部署做不了，且非核心）。

## 二、与现有工具箱的接入方式

沿用现有约定，不发明新模式：

| 约定 | 现状（已确认） | 拼豆工具照做 |
|------|----------------|--------------|
| 注册 | `js/tools/registry.js` 数组，每项含 id/name/icon/desc/loader | 加一条 `perler-beads` |
| 模块 | 导出 `mount(container)` / `unmount()` | 同样导出这两个方法 |
| 挂载 | `main.js` 动态 `import()` loader，调用 `mount`，切换时调 `unmount` | 无需改 main.js |
| 样式 | 全部写进 `css/style.css`（带工具前缀，不注入 `<style>`） | 加 `.perler-` 前缀规则进 style.css |
| 图片处理 | 全程浏览器本地，不上传 | 同样纯本地 |

注册项示例：

```js
{
  id: 'perler-beads',
  name: '拼豆图纸',
  icon: '🧩',
  desc: '上传图片，生成拼豆像素图纸与色号清单，全程本地处理',
  loader: () => import('./perler-beads.js')
}
```

## 三、文件结构

```
js/tools/
├── perler-beads.js            # 主入口：mount/unmount + UI 装配 + 事件
└── perler-beads/
    ├── color-data.js          # 291 色色板 + 5 套色号对照（由参考 JSON 转换而来）
    ├── color-utils.js         # hexToRgb / Oklab 距离 / findClosestColor
    ├── pixelation.js          # 像素化：Dominant / Average 双模式
    └── exporter.js            # 导出 PNG 图纸 / CSV 清单
css/style.css                  # 追加 .perler-* 样式规则
```

样式集中在 `style.css`，与 id-photo / bg-remove 一致，不单独建 css 文件。

## 四、功能范围（分阶段，MVP 优先）

### MVP（第一批，必做）
1. **上传**：拖放 / 点击选择，JPG/PNG，本地读取为 ImageData。
2. **像素化**：横向格子数滑块（10–150，见决策 D2），Dominant / Average 双模式实时切换。
3. **色号映射**：每格颜色映射到最近的拼豆色（Oklab 距离），显示所选色号系统的编号。
4. **预览渲染**：Canvas 绘制像素格 + 网格线；格子上叠加色号文字（可开关）。
5. **色号统计**：列出用到的每种颜色（色块 + 色号 + 数量），按用量排序。
6. **导出**：
   - 带色号标注的 PNG 图纸
   - 颜色清单 CSV（色号、HEX、数量）

### 增强（第二批，可选，视需求再做）
- 手动编辑：着色 / 橡皮 / 颜色替换 + 撤销栈（这是最大的工作量，MVP 先不做，见决策 D1）。
- 排除某些颜色并重新映射到剩余色板。
- localStorage 记住上次的粒度/模式/色号系统偏好。

### 明确不做
- AI 图片优化（需后端）。
- 多色板自定义、模板库、分享链接等参考项目的长尾功能。

## 五、核心技术点

### 5.1 色板数据（color-data.js）
直接采用参考项目 `colorSystemMapping.json` 的原始结构——**以 HEX 为 key**，一色对应 5 套色号：

```js
export const PALETTE = {
  "#FAF4C8": { MARD: "A01", COCO: "E02", 漫漫: "E2", 盼盼: "65", 咪小窝: "77" },
  // ... 共 291 色
};
```

映射时遍历 291 个 HEX 找最近色，所以 HEX 必须是 key，不可改成「系统→色号→HEX」的结构。
从 JSON 生成此文件用一次性脚本完成，不手写。

### 5.2 颜色距离（color-utils.js）
sRGB → 线性 → Oklab，用 Oklab 欧氏距离找最近色（比 RGB 距离更贴合人眼）。纯函数，从
`colorSystemUtils.ts` / `pixelation.ts` 移植。

### 5.3 像素化（pixelation.js）
把原图按格子数切成网格，每格：
- **Dominant**：统计格内出现最多的颜色（适合卡通/清晰边界）。
- **Average**：格内 RGB 平均值（适合照片/渐变）。

输出二维数组 `cells[row][col] = { hex, sys编号 }`，供渲染和统计复用。

### 5.4 渲染性能
逐格 `fillRect` + 文字。上限 150 格时最大约 150×150≈2.25 万格，一次性绘制可接受。
色号文字仅在格子足够大（缩放后 ≥ 某阈值）时绘制，避免小格子糊成一团。
（不做 300 格是刻意的性能取舍，见决策 D2。）

## 六、UI 布局

```
┌──────────────────────────────────────────────┐
│ 顶部工具栏：[上传] [粒度滑块] [模式▾] [色号系统▾] [显示色号☑] [导出▾] │
├──────────────────────────────────────────────┤
│                                                │
│            Canvas 像素预览（可缩放）             │
│                                                │
├──────────────────────────────────────────────┤
│ 色号统计：■A01 ×120  ■B2 ×85  ■C05 ×64 ...      │
└──────────────────────────────────────────────┘
```

移动端：工具栏可换行，Canvas 自适应宽度，统计区横向滚动。

## 七、实施顺序

1. 转换色板 JSON → `color-data.js`（脚本一次性生成）。
2. 移植 `color-utils.js` + `pixelation.js`（纯函数，可先写小测试验证映射正确）。
3. `perler-beads.js` 搭 UI + Canvas 渲染 + 统计，接入 registry。
4. 补 `.perler-*` 样式进 style.css。
5. 导出 PNG / CSV。
6. （可选）第二批增强功能。

## 八、参考资源
- 基础版：https://github.com/Zippland/perler-beads
- AI 版（不采用其 AI 部分）：https://github.com/liangdabiao/perler-beads-ai
- 色板数据：`拼豆参考/perler-beads/src/app/colorSystemMapping.json`（291 色）
- 色号对照：`拼豆参考/perler-beads/色号对应表.csv`

